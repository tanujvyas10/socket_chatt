{
  "name": "socketapp",
  "version": "1.0.0",
  "description": "a simple socket app",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "junat",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^3.0.2",
    "socket.io": "^2.4.0"
  }
}
